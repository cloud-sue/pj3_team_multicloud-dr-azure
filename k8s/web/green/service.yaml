apiVersion: v1
kind: Service
metadata:
  name: web-green
  namespace: app-web
  labels:
    app: web
    color: green
spec:
  type: ClusterIP
  selector:
    app: web
    color: green
  ports:
    - name: http
      port: 80
      targetPort: http
