apiVersion: v1
kind: Service
metadata:
  name: web-blue
  namespace: app-web
  labels:
    app: web
    color: blue
spec:
  type: ClusterIP
  selector:
    app: web
    color: blue
  ports:
    - name: http
      port: 80
      targetPort: http
